# CLAUDE.md — japan-food-access-analysis

このリポジトリで作業するときの前提・手順・落とし穴をまとめる。次回はまずこれを読むこと。

## このリポジトリは何か

**食料品店まで道路距離500m以上のところに住む人口**を、国勢調査 **125mメッシュ人口** ×
食料品店 POI で推計する。農水省「[食料品アクセス](https://www.maff.go.jp/j/shokusan/eat/access_genjo.html)」の
**食料品アクセス困難人口**は突合の相手であって、推計の対象ではない。

### ★ 推計するのは距離条件だけ（2026-08-28 に確定）

農水省の困難人口は3条件（500m以上 **かつ** 自動車利用困難 **かつ** 65歳以上）だが、
**本リポジトリは第1条件しか推計しない**。自動車利用困難率を外部データで固定できておらず、
公表値との比（0.42〜0.66）が自動車要因と店舗レイヤの穴のどちらに由来するか分離できないため。
年齢条件も外せるように **65歳以上と総人口の両方**を出す。
→ **「困難人口」という語を成果物に使わないこと。** 出すのは「500m圏外人口」。
→ **「自動車利用困難率を固定する」は前提から外れた**。必要になるのは公表値そのものを
   再現しようとする場合だけ（そのときの出所は農水省が使う**家計調査個票**）。

- **人口レイヤ・距離判定・公表値突合がここ**。**店舗レイヤは持たない**（外から受け取る）。
- 店舗レイヤの構築は [shiwaku/japan-food-store-master](https://github.com/shiwaku/japan-food-store-master)。
  2026-08-27 にそちらから検証器（`validate_access_difficulty.py` / `fetch_mesh_population.py`）を
  分離して発足。分離元の該当コミットは #7 / #16 / #20 / #27。
- 姉妹プロジェクト [japan-transit-desert-analysis-125](https://github.com/shiwaku/japan-transit-desert-analysis-125)
  （公共交通空白地域・国交省定義）と同じ構造の農水省版。あちらの `01_prepare_facilities.py` が
  担う施設供給を、こちらでは japan-food-store-master が担う。
- **⚠ 参照するのは `-125`（125mメッシュ版）。`-125` の付かない250m版・`-100` 版ではない。**
  3本は別リポジトリで中身が違う。メッシュ人口のファイル名・PMTiles 生成・ビューワは
  すべて125m版に合わせること。

## リポジトリ構成

```
scripts/
  01_fetch_mesh_population.py     e-Stat T001225 → data/mesh/mesh125_pop.parquet
  02_validate_access_difficulty.py 圏外率算出＋農水省 表5 突合 → output/*.csv
docs/
  検証_アクセス困難人口_メッシュ単位.md  一次記録（操作化の判定・カテゴリ感度・誤差）
input/     店舗レイヤの置き場（.gitignore。japan-food-store-master の parquet）
data/      メッシュ人口・境界・農水省表5（.gitignore。全部スクリプトが自動取得）
output/    市区町村別・カテゴリ感度の CSV（追跡する）
```

## 作業ワークフロー（厳守）

- **main へ直接コミット・push しない**。必ずブランチを切って PR を作成しマージする。
  ```
  git checkout -b <type>/<topic>      # feature/ fix/ docs/ 等
  git push -u origin <branch>
  gh pr create --base main --head <branch> --title "..." --body "..."
  gh pr merge <n> --squash --delete-branch
  git checkout main && git pull --ff-only origin main
  ```
- コミットメッセージ末尾に `Claude-Session: <url>` を付ける。

## ★ 判定の原則（これを外すと全部無意味になる）

**件数を足せば圏外率は必ず下がるので、下がったこと自体は判定材料にならない。**
店舗ソースの投入可否は次の2つで見る（`02_validate_access_difficulty.py` が両方出す）:

1. **比 農水省÷本推計 が1を超えないこと**
2. **農水省公表値との相関 r が上がること**

**比の値そのものは推計の精度ではない**（公表値は3条件込みなので、比は「暗黙の自動車利用困難率」）。
使えるのは「1を超えたら論理矛盾」という必要条件のチェックだけ。
店舗ソースの比較は**変種Aで見る**（変種Dは道路距離の再計算が要るうえ比>1 が出やすい）。

**どちらも「地方部＝高齢者密度 < 1,500人/km²（可住地ベース）」に限定して見る**
（1,586市区町村・高齢者 2,251万人）。理由は2つ:

- 都市部は操作化そのものが破れている（密度1,500以上では比>1 が 44〜97%、未満では約1%）
- 全国 r は都市-地方のコントラストで稼いでいて、**純増が都市部に多いソースは地方の穴を
  1つも埋めなくても r を動かせる**

**区分に圏外率を使わないこと。** 圏外率は測る対象そのものなので、店舗レイヤを差し替えると
区分も動いて比較にならない。密度は**店舗レイヤに一切依存しない**（検証器が
`高齢者密度_人per_km2` を出す）。

**3県だけで判定してはいけない**。47県で測ると結論が逆転した実例が2つある:
- 「変種Aは全市区町村で比≤1」→ 全国では102件破れる
- 「生鮮3業種を先に入れろ／⑪ は後回し」→ **地方部では生鮮が悪化・⑪ が中立で逆**

## ★ 農水省の推計方法（2026-08-28 判明・issue #4）

農林水産政策研究所 研究成果報告会資料
[240319_01.pdf](https://www.maff.go.jp/primaff/koho/seminar/2023/attach/pdf/240319_01.pdf)
スライド5に明記されている。

> - 人口のある500mメッシュ単位に、当該及び周辺メッシュの店舗状況から、
>   **最も近い店舗が500m以上である確率**を算出。
> - **この確率に人口を乗じて**、メッシュごとに店舗まで500m以上の人口を推計。

図は「各メッシュにおける500m以上の確率 × 半径500mの円」で、3×3メッシュに
0.939 / 0.828 / 1.000 / 0.385 / **0.028** / 0.955 / 0.598 / 0.259 / 0.989、「メッシュ22内の位置k」。

**判定は二値ではなく確率（面積比）。** ここで比べてきた変種A/C/D は全部二値なので構造が違う。
**店舗のあるメッシュでも確率は0にならない**（図の中心 0.028）ので、千代田区のような
「圏内なのに困難人口」が自然に出る。従来推計の店舗データが**商業統計メッシュ統計＝
メッシュ別件数（座標なし）**なので、店舗位置もメッシュ内で不確実として扱っている、という読み。

| 項目 | 内容 |
|---|---|
| 距離 | **直線500m**（道路距離ではない） |
| 店舗データ | 2020年推計は**2021年スーパー名鑑等**（〜2015年は商業統計メッシュ統計） |
| 業種 | 2020年推計で酒・菓子パン小売業を**除き**ドラッグストアを**追加**（現在の `cat` 4分類と整合） |
| 自動車利用率 | **家計調査個票**（〜2015年は全国消費実態調査） |

**issue #4 で否定された仮説**: 年次のズレ（2021年データなので現在のPOIと近い）・業種範囲。

## 二値近似の中での操作化（地方部でのみ整合する）

「500m以上」の実装候補を比べた結果、**同一500mメッシュ内の店舗存否（変種A）だけが整合する**。
**ただし全国で見ると都市部で破れる**（2026-08-27・issue #2。後述）。

| 変種 | 操作化 | 圏外率 | 比 中央値 | 比>1 |
|---|---|---:|---:|---:|
| **A** | **同一500mメッシュ内に店舗があるか** | 62.4% | 0.365 | **0件** ✅ |
| B | 自メッシュ+8近傍メッシュ | 26.6% | 0.592 | 多数 ❌ |
| C125 | 125mメッシュ重心から実距離500m以内 | 40.5% | 0.501 | 5件 ❌ |

- **125mメッシュにしても変種Cの不整合は消えない**（Phase1で5件・ATP基準＋許可で10件・max 4.237）。
  当初は500m重心の粗さ（最大 ~350m ずれ）を疑ったが、~88m まで落としても比>1 が残る。
  → **操作化そのものが違う**。直線500m円ではない。この論点は決着済み。
- 相関 r だけ見ると変種C125 の方が高い（0.499 vs 0.451）が、**論理整合を満たさないので採らない**。
- **隣メッシュの店舗には救われない**＝1店の欠落が1メッシュ全員を圏外に振り替える。
  だから**店舗レイヤの網羅性が距離指標に直に効く**。

## ★ 47県での実測（1,740市区町村・65歳以上 35,314,481人・2026-08-27）

農水省 表5 の全国計 9,042,736人・25.6% と突合（1,740/1,740）。

| 系統 | 圏外率A | 比 | **比>1** | r（全国） | **r（地方部）** |
|---|---:|---:|---:|---:|---:|
| Phase1（102,984店） | 51.7% | 0.496 | **44件** | 0.305 | 0.177 |
| ATP基準＋許可（139,492店） | 45.2% | 0.567 | **102件** | 0.271 | 0.158 |

**破れは都市部に集中する**（圏外率A 30%で分ける）:

| 領域 | 市区町村 | 高齢者 | 比>1（ATP+許可） | 破れ率 |
|---|---:|---:|---:|---:|
| 都市部（圏外率A<30%） | 119 | 1,011万 | 88 | **73.9%** |
| **地方部（圏外率A≥30%）** | 1,621 | 2,521万 | 14 | **0.9%** |

千代田区は変種A圏外率 **0.1%** に対し農水省 4.5%（比 37.6）。密な市街地では全500mメッシュに
店舗があるので圏外が消えるが公表値は数%ある＝**「圏内なのに困難人口」**。変種C125（実距離500m）は
圏外率がさらに下がるので**もっと悪化**（比>1 465件・max 695）＝「もっと緩い判定」に答えは無い。

**⚠ ATP基準＋許可の投入は全国で測ると悪化している。** 3県では「比≤1 維持・r 0.448→0.462 改善」で
投入が正当化されたが、47県では **比>1 が 44→102件に倍増し r は 0.305→0.271 に悪化**する。
純増が都市部に偏っているため。japan-food-store-master #43 / #44 の判断は測り直しが要る。

## 参考：3県での実測（88市区町村・65歳以上 1,107,739人）

| 人口レイヤ | 店舗レイヤ | 圏外率A | 比 | r | 比>1 |
|---|---|---:|---:|---:|---:|
| 500mメッシュ | Phase1（102,984店） | 62.2% | 0.420 | 0.448 | 0件 |
| 500mメッシュ | ATP基準＋許可（139,492店） | 57.3% | 0.456 | 0.462 | 0件 |
| **125mメッシュ** | Phase1 | 62.4% | 0.416 | 0.451 | 0件 |
| **125mメッシュ** | ATP基準＋許可 | 57.4% | 0.452 | 0.454 | 0件 |

**変種Aは人口を125mにしてもほぼ動かない**。500m単位の判定は同じで、子16メッシュの和が親に一致するため。
変わるのは市区町村境での割当精度だけ（65歳以上人口 1,101,938 → 1,107,739 = +5,801人）。

**判定**: **地方部の**相対比較・スクリーニングには耐える（地方部1,621市区町村で比>1 は0.9%）。
**都市部では操作化そのものが破れている**（未解決の宿題）。
**絶対人数の推計には耐えない**（係数が自動車利用困難率と店舗の穴のどちらに由来するか分離できていない。
市区町村単位の店舗由来誤差は概ね ±20〜30%）。

### 次の一手（優先順）

1. **確率版（変種E）を実装して都市部を再現できるか測る**（issue #4）。
   農水省の推計方法は **2026-08-28 に判明済み**（後述「農水省の推計方法」）。
   二値ではなく**確率＝メッシュ内位置についての面積比**なので、変種A/C/D とは構造が違う。
2. ~~自動車利用困難率を外部データで固定する~~ → **前提から外れた**（距離条件だけの推計にしたため）。
3. ~~検証県を47都道府県に拡張~~ → **2026-08-27 に完了**（issue #2）。
4. 店舗レイヤの穴埋めは japan-food-store-master 側の課題。効果は必ずここの検証器で
   **47県・地方部限定の指標**で測る。3県での判定は当てにならない。
   実測済みの結論（issue #46）: **ATP基準への組み替えは改善、許可⑪ は中立、生鮮3業種は悪化**。
   生鮮の破れは離島・極小自治体に集中する（高齢者千人未満の120市区町村で6.7%）。

## 再現の要点

```bash
python scripts/01_fetch_mesh_population.py
# 主指標（変種D）の道路距離。**02 と同じ店舗レイヤで作ること**
FOOD_STORES=input/food_store_master_atp_super.parquet python scripts/04_road_distance.py
FOOD_STORES=input/food_store_master_atp_super.parquet OUT_SUFFIX=_ATP⑪ \
    python scripts/02_validate_access_difficulty.py 高知県 島根県 宮城県
```

- 店舗レイヤに必要な列は **`lat` / `lng` / `cat`** の3つだけ。
  `cat` は `supermarket` / `convenience` / `drugstore` / `fresh_food`。
- `FOOD_STORES` / `OUT_SUFFIX` で入出力を差し替えられる。**既存 CSV を実験で上書きしないこと**。
- **`04` と `02` の店舗レイヤは必ずそろえる**（`02` は道路距離を parquet から結合するだけで、
  中身が別レイヤでも黙って通る）。`04` を飛ばすと変種Dの列が落ち、変種A/C125 だけで走る。
- 47県で回すときの店舗レイヤは **`food_store_master_atp_super.parquet`（ATP基準＋許可⑪・122,249店）**。
  地方部の実測で最良（issue #46。生鮮3業種は悪化するので入れない）。
- 外部データは全部スクリプトが自動取得・キャッシュ（`data/mesh/` `data/boundary/`
  `data/maff_2020_table05.xlsx`、いずれも .gitignore）。

## 道路距離（変種D・scripts/04_road_distance.py）

**算出方法は japan-transit-desert-analysis-125 の `02_calc_transit_desert.py` を踏襲する。**
店舗を最近傍1道路ノードにスナップ → Multi-source Dijkstra → アクセスリンク L6 でメッシュ距離に変換。
walk 3.6km/h = 60m/分、500m = 8.33分。到達不能は圏外に倒し、アクセスリンクが無いメッシュは落とす。

- ネットワークはリポジトリ外。既定は
  `ksj/ksj-roadcenterline-route-search/01_MakeNetwork/nationwide_walk/`（`NETWORK_DIR` で変更可）。
  道路リンク 24,045,959 / ノード 18,614,424 / **アクセスリンク L6 23,740,720**。全国126秒。
- **有効ノード 18,441,645 が -125 の記録値と一致するか**を毎回確認すること。ズレたら
  ネットワークか成分フィルタが違う。
- **駅の4象限スナップはしない**（線状障害物が店舗には無いため。-125 のバス停と同じ最近傍1ノード）。

### ★ 変種の使い分け（実測で確定・issue #2 / #4）

**主指標は変種D**（2026-08-28 に確定）。推計の対象が「距離条件だけ」になったので、
実際に歩く距離を測る変種Dが本命。変種Aは対照。

| 目的 | 変種 | 根拠 |
|---|---|---|
| **本推計（どの地域が実際に厳しいか）** | **D（道路距離）** ★主 | 実距離。相関が最良（地方部 rA 0.199 → **rD 0.258**） |
| 農水省の公表値を再現する（もはや目的ではない） | A（メッシュ存否） | 地方部で比≤1 を満たす |
| — | C125（直線500m） | どちらでも劣る。対照としてのみ残す |

**道路距離は都市部の破れを解消しない**（比>1 が 78→173件）。メッシュ存否のほうが厳しく
（実効半径250m程度）、道路距離500mだと圏外が減って比が上がるため。**予測を外した実例**なので、
「もっと厳しい／緩い」の向きは必ず実測で確かめること。原因は確率版との構造差（上記）で、
二値である限りどちらの向きにも答えは無い。

**変種Dの分母は道路距離が定義できるメッシュに限る**（`outD is not null`）。アクセスリンクが
無いメッシュは道路距離側に現れないので、分母をそろえないと率が濁る（全国1,853メッシュ・
65歳以上 8,450人＝0.024%）。9.2 の rD 0.334 / 比>1 172件 が 0.337 / 173件 になったのはこの修正。

### 47県の推計値（ATP⑪ 122,249店・1,740市区町村・2026-08-28）

| 判定 | 総人口 | 割合 | 65歳以上 | 割合 |
|---|---:|---:|---:|---:|
| **★D 道路距離500m以上** | **41,333,085** | **32.8%** | **13,668,822** | **38.7%** |
| A 同一500mメッシュに店舗なし | 52,368,599 | 41.5% | 16,767,203 | 47.5% |
| C125 直線500m以上 | — | — | 9,070,750 | 25.7% |

母数 総人口 126,088,473 / 65歳以上 35,314,481人。地方部（密度<1,500）は
D圏外率 52.0% / rD 0.258 / 比D>1 50件（3.2%）、都市部は D圏外率 15.3% / 比D>1 123件（80%）。
**地方部でも比D>1 は変種Aの8倍出る**（6→50件）。破れは密度1,000〜1,500の郊外自治体に寄る。

## ビューワ（docs/）

- 作りは **japan-transit-desert-analysis-125** の `docs/index.html` を踏襲。
  住所検索（地理院API）・2D/3D・凡例/集計トグル・ボトムシート・iOS の16px対策まで同じ。
- **区分はタイルに焼いていない**。`out_a` / `out_d` と `sm` から MapLibre の式で導く
  （`catCodeExpr()`）ので、区分の定義を変えてもタイルを作り直さなくてよい。
- **既定は変種D（道路距離）**。右上のボタンで変種A ↔ 変種D を切り替えると、
  色・凡例・**集計パネルの数値**・判定方法の説明・ポップアップ・住所検索がまとめて追従する。
  集計の数値は `MODE_STATS` にハードコードしてあるので、**再集計したら両方の系列を更新すること**。
- **`#stats` だけ `backdrop-filter` を外してある。** モバイルで `overflow-y:auto` になり、
  **内部スクロールを持つパネルに backdrop-filter を掛けると実GPUで合成不具合（白化・欠け）**が出る
  （japan-food-store-master の CLAUDE.md 既知の落とし穴）。-125 は掛けているが踏襲しない。
- PMTiles は `-Z4 -z13 --no-tile-size-limit --no-feature-limit --coalesce-densest-as-needed -P`
  で -125 と同じ。2,814,449 ポリゴンで約300MB。**gitignore なので公開時は外部ホスティング**に置く。
- ローカル確認は `python serve.py 8080` → http://localhost:8080/docs/
  （PMTiles は Range リクエストが要るので `python -m http.server` では不可）。

## 落とし穴・環境メモ

- **e-Stat メッシュ統計の列番号**: `…019` が65歳以上、`…022` は**75歳以上**。取り違えると分母が半分になる
  （仙台市 65+ = 255,567 が 126,553 になる）。**125m（T001225）と 500m（T001141）で並びは同一**。
- **e-Stat 統計GIS は appId 不要**でダウンロードできる。125mメッシュ人口は
  `statmap-search/data?statsId=T001225&code=<1次メッシュ>&downloadType=2`。境界も同様。
  e-Stat **API** の appId はユーザー保有で、リポジトリには無い。
- **125mメッシュは秘匿が桁違いに多い**。全国 2,820,831 メッシュ中 **1,017,247 メッシュ**が
  65歳以上 `*`（秘匿）。`HTKSYORI=2` の行は `HTKSAKI` メッシュに合算済みなので `*` は0扱いで
  総計は保たれるが、人口が合算先メッシュの位置に寄る。県計・市区町村計の目減りは必ず確認すること。
- **DuckDB の `/` は DOUBLE を返し `::int` が四捨五入する**。メッシュ添字は必ず `//`（整数除算）を使う
  （`(m-1)/2` で m=2 が 0.5→1 に丸められ別メッシュと衝突した）。
- **DuckDB spheroid バグ**: この環境では `ST_Distance_Spheroid` / `ST_DWithin_Spheroid` が `-nan` を返す。
  距離は等距円筒近似（緯度補正した平面距離）で代替する。
- **`ST_Read` の戻り型は `GEOMETRY('EPSG:4612')`** で、そのままでは rtree インデックスが作れない。
  `geom::GEOMETRY` で素の型に落とす。
- **e-Stat 境界 shapefile の DBF は CP932**。`CITY_NAME`/`PREF_NAME` を読むと DuckDB が unicode エラーを
  出す。ASCII のコード列（`PREF`,`CITY`）のみ読み、名称は農水省の表5から引く。
- **政令指定都市**: 農水省 表5 は市単位（仙台市=04100）、境界データは区単位（04101…）。
  3桁目が `1` のコードを `XX100` に寄せる。
- **11桁L6コードの先頭9桁がそのまま500mメッシュコード**になる。変種Aの判定キーはこれ。
- **区コードを市へ寄せる規則を自前で書かないこと**。`XX1YY → XX100` は**間違い**で、
  農水省 表5 は東京23区を個別コード（13101〜13123）で持つので寄せてはいけないし、
  政令市の市コードも 100 とは限らない（横浜 14100・**川崎 14130**・相模原 14150・浜松 22130・
  堺 27140・福岡 40130）。川崎区(14131) を 14100 に寄せると横浜市に合算される。
  **表5 を正とし、表5 に無いコードだけを「同一県内で自分以下・末尾0 の最大の表5コード」へ寄せる**。
  3県には仙台市しか無かったのでこのバグは 2026-08-27 の47県拡張まで露見しなかった
  （東京23区の65歳以上 2,028,298人が丸ごと欠落していた）。
- **メッシュ人口は既定で `input/2020_pop_census_mesh125.parquet` を読む**
  （姉妹リポジトリ japan-transit-desert-analysis-125 と同じ流儀・同じファイル名）。
  全国が1本に入っているので県名の指定は不要。e-Stat からの取得はファイルが無いときのフォールバックで、
  全国だと1次メッシュ151個・数十分かかる。
- Windows の Git Bash でコンソールが文字化けするときは `PYTHONIOENCODING=utf-8` を付ける
  （ファイル出力は影響を受けない）。
